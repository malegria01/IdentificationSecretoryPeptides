name: signal_peptides_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - jupyter
  - pandas
  - numpy
  - matplotlib
  - seaborn
  - biopython
  - pip
  - pip:
      - biopandas
      - pdb2sql
